<UserControl x:Class="Assignment.Views.TotalProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Assignment.Views"
             mc:Ignorable="d" Height="Auto" Width="Auto" Padding="5">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition MinWidth="200" Width="*"/>
            <ColumnDefinition Width="60"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Content="Total progress" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
        <ProgressBar Grid.Column="1" Minimum="0" Maximum="100" Value="{Binding TotalProgress}"/>
        <Label Grid.Column="2" Content="{Binding TotalProgress}" HorizontalAlignment="Center" VerticalAlignment="Center" ContentStringFormat="{}{0:0.00}%"/>
    </Grid>
</UserControl>
